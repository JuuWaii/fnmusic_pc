# 原生客户端开发清单

更新：2026-09-22。

当前需求的统一排序、依赖与验收边界以 [产品路线图](PRODUCT_ROADMAP.md) 为准。本文件保留具体开发和验收状态。

## 当前交互修复

- [x] 播放进度拖动：拖动预览与播放器进度更新隔离，松手只发起一次跳转。
- [x] 双击歌曲播放：播放被双击的行，支持从当前播放曲目切换到另一行。
- [x] 合成音频窗口实测暂停拖动、恢复后继续前进及双击切歌。
- [x] 28 项原生回归测试，包括 DPAPI、FFmpeg 和队列规则。
- [ ] 扩展拖动回归：真实大文件、连续快速拖动、键盘、触摸、超时后恢复。

## 阶段 4 后续

- [x] 自动续播与四种模式合成窗口基本交互验收，见 QUEUE_UI_ACCEPTANCE.md。
- [x] 歌曲搜索、分页及清空代码接入；28 项原生检查通过，范围见 NATIVE_SEARCH.md。
- [x] 搜索真实 NAS 只读验收及完整结果集合分页兼容。
- [x] 回车修正、空结果、多页及清空后迟到旧请求的合成窗口验收。
- [ ] 扩展搜索回归：更多输入法组合、真实 NAS 连续快速查询及网络超时。
- [x] 专辑/歌手列表、基础详情、详情歌曲分页与当前页播放；33 项原生检查和基本合成窗口验收通过。
- [x] 专辑/歌手真实 NAS 列表、详情和歌曲分页只读验收，11 项通过，无跳过。
- [x] 专辑/歌手多页、返回原页、空详情鼠标验收；16 项窗口内状态回归包含双向迟到详情隔离，见 NATIVE_COLLECTIONS.md。
- [x] 修复迟到 401 清理新会话；35 项原生检查及 24 项窗口状态检查通过，覆盖合成网络失败重试、迟到失效和当前会话清理。
- [ ] 实际网络断连、失效会话及连续快速操作的扩展鼠标回归。
- [x] 歌手关联专辑分页列表、进入专辑详情和逐级返回原页。
- [x] 歌手关联专辑真实 NAS 只读验收：列表、分页、专辑详情与歌曲读取通过。
- [x] 歌手关联专辑入口、播放及逐级返回原页的鼠标交互回归；返回时播放和队列保持。
- [x] 收藏列表、收藏与取消收藏基础接入，38 项原生检查通过，见 NATIVE_FAVORITES.md。
- [x] 收藏合成增删、失败重试、列表刷新及队列保留状态回归；窗口检查共 34 项通过。
- [x] 真实收藏只读基础验收：会话、第一页、收藏标记与重复读取通过。
- [ ] P0：收藏鼠标交互、真实多项收藏分页与适当范围写入验收；当前数据不足两首，分页未标通过。
- [ ] FFmpeg 自定义 AVIO 随机读取，改善大文件跳转并扩展容器格式覆盖。

## 后续阶段

- [ ] FN Connect、OAuth、HLS、断线恢复。
- [ ] 设备热插拔、托盘、快捷键、媒体键和睡眠定时。
- [ ] 固定 FFmpeg 分发版本、许可与源码材料；安装升级和多轮隐私审查。

## 新需求统一排期（2026-09-22 重排）

此前低优先级需求与本轮新增需求已合并；以下顺序依赖产品路线图，不再把所有歌单功能统一放在末尾。

- [ ] P0：统一音乐源、音乐库、曲目引用及 Provider 能力模型。
- [ ] P1：本地音乐导入、扫描、播放及音乐库分类管理。
- [ ] P1：跨音乐库本地歌单；同级展示飞牛专属同步歌单，支持客户端创建与网页端双向刷新同步。
- [ ] P1：飞牛素材库、用户及服务端非 UI 设置的可支持项同步，保存后回读、处理冲突。
- [ ] P1：浅色、深色和跟随 Windows 系统三种主题模式，手动偏好持久化。
- [ ] P2：Navidrome、Emby、自定义通用音乐源，逐源核验浏览、搜索和播放。
- [ ] P2：歌曲/专辑封面、应用内歌词与播放时间轴同步。
- [ ] P2：Windows 系统媒体信息与控制；可发现的本机音乐/歌词信息接口和消费端联调。
- [ ] P3：参考 Apple Music、网易云音乐、飞牛音乐及调研到的开源客户端优化 UI。
- [ ] P3：桌面歌词开关、位置和显示偏好，与播放进度同步。

## 版本控制约定

在改动构成可编译、已验证的完整节点时提交到本地仓库。提交范围明确，不混入私人需求、浏览器资料、令牌、测试原始记录或构建产物；既有未纳入本次范围的改动保留。GitHub 发布另按发布阶段授权执行。

## 2026-09-13 新增

- [x] 队列管理：查看、播放所选、移除、清空；领域回归及 Release 编译通过。
- [x] 队列弹窗合成音频真实交互验收：跨曲尾保持打开、删除当前项、清空后重建。
- [x] 按用户要求提前归档 Electron，移除旧构建与发布入口；发布验收仍未完成。
