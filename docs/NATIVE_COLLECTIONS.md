# 专辑与歌手浏览

更新：2026-09-20。

## 当前实现

- 音乐资料库新增歌曲、专辑、歌手入口。专辑和歌手列表每页 50 项，可点击条目或选择后点击“查看详情”。
- 详情显示名称和歌曲数量，歌曲独立分页获取；返回列表恢复进入详情前的页码。缺少歌曲数量时显示未知，不当作零。
- 详情歌曲复用按钮播放、双击播放和当前页队列；浏览、返回或切换类别不改变已有队列。队列来源显示专辑/歌手详情当前页。
- 搜索框始终搜索歌曲；提交搜索或清空会返回歌曲视图。刷新加载当前类别的第一页。
- 列表、详情和搜索共享曲库请求取消源；响应写回前检查取消和窗口关闭。会话变化清空浏览状态，播放请求保持独立。

## 已审查的静态契约

证据来自本机缓存的 NAS 前端 API 模块和专辑/歌手页面模块。未复制签名材料或用户数据。

| GET 路径 | 参数 | 响应消费方式 |
| --- | --- | --- |
| album/list、artist/list | page、size | data.list、data.total；条目 guid、name、可选 trackCount |
| album/detail | guid | 直接专辑对象，或 data.album 包装 |
| artist/detail | guid | 直接歌手对象 |
| track/album-detail/list | albumGUID、page、size、sort=trackNo,asc | list、total，沿用曲库曲目映射 |
| track/artist-detail/list | artistGUID、page、size | list、total，沿用曲库曲目映射 |

复用现有认证、重定向拒绝、15 秒超时和 2 MiB 响应限制。列表条数超过请求大小或 total 时拒绝，当前实例实测支持服务端分页，无需套用搜索接口的完整集合兼容规则。

## 本轮验证

- 33 项原生检查通过，包括正常用户 DPAPI。新增 5 项覆盖两类列表路由和分页、详情包装与 ID 编码、详情歌曲路径与排序、非法响应和错误 ID、非法参数不发请求及空集。
- Debug、Release 构建均 0 警告、0 错误。Debug 使用独立 `.review-audit/collections-ui-build/` 输出。
- `--preview-synthetic` 窗口实测：专辑列表 → 查看详情 → 播放 A → 返回列表；A 持续播放，专辑队列 1/2 不变。
- 歌手列表点击条目进入详情，双击 B 切歌，队列变为歌手详情 2/2；返回歌曲页后 B 持续播放。合成模式默认静音、不读取账户、不联网。

## 2026-09-16 真实 NAS 只读验收

新增仅 Debug 显式运行的 `--verify-native-collections`，使用本应用保存的会话，仅执行 GET。检查两类列表第一页与第二页、详情 ID/名称一致性，以及详情歌曲第一页与第二页的条数、总数和不同 ID。首个集合不足两首时，最多读取前 50 个集合，选择多曲目集合；数据不足明确记录跳过，不视为分页通过。

最终报告时间为 `2026-09-16T15:17:29.052315+00:00`，stage=complete，11 个检查项全部为 true，skipped 为空。进程已退出，并核对报告晚于本次启动。报告只包含固定阶段、布尔值、跳过原因和异常类型，不含地址、凭据、曲名或集合 ID。输出位于 `.review-audit/collections-live-build/collection-verification.json`，不提交原始数据或构建产物。

Debug/Release 构建均 0 警告、0 错误。本轮只增加验证入口，未修改生产接口实现，未重复运行上一节点的 33 项原生检查；真实 API 验收不能替代完整窗口回归。

## 尚待完成

- 当前实例接口已通过；服务器版本升级后需重新验收。
- 实际网络断连与更多连续快速操作的鼠标回归；合成故障状态验证见下文。
- 封面、发行年份及更多详情信息。

## 2026-09-20 歌手关联专辑

歌手详情新增“歌手专辑”，按歌手加载专辑分页列表，可进入专辑详情并复用现有歌曲播放路径。返回依次恢复关联专辑列表页、歌手歌曲页、歌手列表页；切换主类别、搜索或会话变化会撤销该导航上下文，已有播放队列不随浏览改变。

缓存前端已确认 `artist.albums` 对应 GET `album/artist-detail/list`，参数为 `artistGUID、page、size`（前端另有可选 sort）。原生复用专辑解析、认证、大小限制和取消保护；新增契约回归核对参数编码、分页、空集、非法参数不发请求和畸形响应。

36 项原生检查通过，Debug/Release 均 0 警告、0 错误。新增窗口内状态断言覆盖关联专辑列表、专辑详情、返回专辑列表第二页及恢复歌手歌曲第二页。该接口本轮尚未请求真实 NAS，新入口的鼠标验收也未执行。

窗口状态报告 `.review-audit/artist-albums-build/collection-window-verification.json`，时间 `2026-09-20T09:56:29.8601099+00:00`，stage=complete，28 项全部通过；进程已退出且报告时间已核对。

## 2026-09-20 歌手关联专辑真实验收

扩展已有 Debug `--verify-native-collections` 入口，使用应用保存的会话，只读请求同一歌手的关联专辑第一页和第二页，核对条数、总数及不同 ID，再读取首个关联专辑的详情与歌曲。数据不足时显式记录跳过，不把单项集合当作分页通过。

本轮报告 `.review-audit/artist-albums-live-build/collection-verification.json` 时间为 `2026-09-20T10:20:42.625982+00:00`，stage=complete，15 项全部为 true，skipped 为空。进程已退出，报告新鲜度已核对；报告不包含地址、凭据、歌手、专辑名称或 ID。

当前实例确认关联专辑接口支持服务端分页，无需兼容修正。Debug/Release 构建均 0 警告、0 错误。生产功能未变，未重复执行上一节点的 36 项原生检查和 28 项窗口状态回归；本轮未执行鼠标验收。

## 2026-09-20 歌手关联专辑鼠标验收

使用 `.review-audit/artist-albums-live-build/` 的合成窗口（`--preview-synthetic --preview-collections`），不读取真实账户、不访问 NAS，默认静音。初次输入缺少窗口坐标信息，重新绑定并捕获窗口后恢复。

实际点击路径：歌手列表第二页 → 合成歌手 051 → 歌曲第二页 → “歌手专辑” → 关联专辑第二页 → 合成专辑 051 → “播放所选”。专辑第一页建立 50 首队列。

随后依次点击“返回列表”“返回歌手”“返回列表”，分别恢复关联专辑第二页、歌手歌曲第二页、歌手列表第二页；末页下一页禁用。三次返回时播放状态均为正在播放，进度观察到 18、23、26 秒，队列一直为来自专辑详情当前页的 1/50。

本轮未发现新缺陷，仅更新验收记录；未修改代码、重复构建或运行自动测试。未覆盖所有窗口尺寸、输入设备和网络异常组合。

## 2026-09-16 多页与空详情窗口回归

新增 Debug 合成参数 `--preview-synthetic --preview-collections`：两类列表各 53 项，第 51 项有 53 首歌曲，第 52 项为空，第 53 项模拟忽略取消的 20 秒详情请求。只使用合成内容，不读取账户、不联网。

鼠标验收在窗口捕获重试恢复后通过：专辑、歌手均可从列表第二页进入详情，歌曲第二页仅有 051–053，末页下一页禁用；返回恢复列表第二页。两类空详情显示“当前详情暂无曲目。”且两个分页按钮禁用。

同时新增 `--verify-collection-window`，须与上述两个参数一起使用。它在真实 WinUI 窗口内调用生产加载、导航方法，断言真实控件状态，完成后退出。返回按钮和回归共用 `ReturnToCollectionListAsync`，不另写返回逻辑。该入口不模拟鼠标，不替代鼠标验收。

报告 `.review-audit/collections-state-build/collection-window-verification.json` 时间为 `2026-09-16T15:40:06.2194657+00:00`，stage=complete，16 项全部通过：两类列表首末页、详情首末页、返回原页、空详情、加载状态与双向迟到详情隔离。延迟请求发出后立即切换类别，等待旧请求完成再检查，标题、列表与分页状态均未被覆盖。

Debug/Release 构建均 0 警告、0 错误。本轮扩展合成夹具并提取原有返回方法，未改动 NAS 契约；未重复执行既有 33 项核心检查。

## 2026-09-16 会话失效与网络恢复

修复底层会话竞态：请求发出时捕获令牌及会话版本，HTTP 401 或业务码 120001 只可清除同一版本的会话；已取消请求不触发会话清理。旧会话的迟到失效响应返回可重试失败，避免上层把新会话判为失效。当前会话的有效失效响应仍正常清理令牌。

新增原生回归覆盖 HTTP/业务失效响应晚于会话替换返回，以及普通网络错误后请求仍携带原会话并成功重试；合计 35 项原生检查通过，包含 DPAPI。Debug/Release 均 0 警告、0 错误。

窗口内合成故障入口扩展检查网络失败提示、清空旧列表及禁用分页、重试恢复、迟到失效响应不清空新页面和队列，以及当前会话失效时列表、标题和队列清空。该入口不读取真实会话、不联网，不代表实际断网或退出真实 NAS 的鼠标验收。

本轮报告位于 `.review-audit/session-recovery-build/collection-window-verification.json`，时间 `2026-09-16T15:47:05.5951555+00:00`，stage=complete，24 项全部为 true。进程已退出且报告时间晚于启动；仅固定检查项写入报告。
