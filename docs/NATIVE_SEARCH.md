# 原生歌曲搜索：实现与验收

更新：2026-09-15。

## 实现

- 搜索按钮及 Enter 提交，清空或空白关键词恢复曲库第一页；翻页、刷新使用已提交的关键词。
- GET `search/track?q=…&page=…&size=…`，关键词 Trim 后按查询参数编码，长度上限 256。复用曲库曲目解析、鉴权及响应大小限制。
- 曲库/搜索请求使用独立取消源。新搜索撤销旧请求，旧响应不能写回列表；切歌不会取消搜索。会话变更及关闭窗口撤销请求。
- 每次搜索开始清除旧列表并禁用分页；错误提示可重试，空结果有明确提示。搜索及清空不改变已经建立的播放队列。
- 从搜索结果播放时固定当前页快照，并显示搜索队列来源。

## 接口证据与验证

缓存的已审查 NAS 前端调用为 `search.track({q: query, page, size})`，消费 `list` 与 `total`，歌曲继续使用同一映射。这是静态接口证据，不是当前 NAS 实测承诺。

26 项原生检查通过，新增 3 项搜索测试覆盖中文和保留字符编码、分页参数、时长映射、非法输入不发送 HTTP、空结果、401 和畸形响应。Debug/Release 编译均为 0 警告、0 错误。

合成窗口已实测：输入 b 后点击搜索，列表只剩 B；播放结果显示搜索队列 1/1；清空后恢复 A/B 曲库，B 保持播放且队列仍为 1/1。

回车实测未触发原有普通 KeyDown 绑定，已改为通过 AddHandler 接收处理过的路由键盘事件，编译通过。修正后实机复测因窗口捕获与目标应用不一致而暂停，不能标为通过。空结果 UI、搜索分页 UI、快速连续搜索与输入法组合输入仍需补验；API 空结果与分页已有合成契约测试。

## 待授权的真实 NAS 验证

`--verify-native-search` 为仅 Debug 启用的显式只读检查：使用本应用加密会话，读取一首曲目，在内存使用其标题搜索，核对分页及无结果查询。报告只含时间、固定检查项、阶段及异常类型，输出到对应 Debug bin 的 `search-verification.json`，不输出关键词、曲名、地址或会话。

自动审批拒绝了该执行请求，理由为用户未在本轮明确授权使用已保存会话访问私人 NAS 曲目。检查未执行；后续需明确授权后再运行，并确认进程退出及报告时间晚于测试开始。不得通过其他会话或工具绕过该限制。
