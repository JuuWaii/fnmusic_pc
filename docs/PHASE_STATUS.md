# WinUI 3 迁移进度

最后更新：2026-09-13。

| 阶段 | 状态 | 证据/限制 |
| --- | --- | --- |
| 0：分析大纲 | 完成 | `WINUI3_MIGRATION_PLAN.md` |
| 1A：认证与原始流探查 | 完成当前子任务 | `api/NAS_API_CONTRACT.md`；用户手动登录后实测曲库、元数据、FLAC 206 |
| 1B：传输与媒体探查、原生边界决策 | 已完成当前子任务，原生验收待后续 | 非零 Range、浏览器 FLAC 解码/seek、HLS 创建/资源/结束及刷新恢复已实测；原生登录/OAuth、重启会话和远程仍待验证 |
| 2：最小原生工程 | 完成 | Debug/Release 编译、中文原生窗口、真实连接/密码登录/进程重启恢复均已验证 |
| 3：原生曲库与播放 | 核心链路通过 | 真实 NAS 会话恢复、播放、暂停、跳转、设备切换与停止清理通过集成验证；界面交互回归继续保留 |
| 4：完整音乐功能 | 进行中 | 已接入当前页队列、手动切歌、自动续播和四种播放模式；搜索、专辑、歌单等待后续 |
| 5–7 | 尚未启动 | 远程适配、桌面体验、审查和发布依大纲推进 |

## 本轮新增

- 脱敏接口契约与进度记录。
- 浏览器只读诊断脚本与 5 项针对性测试。
- 已验证主业务前缀 `/music/api/v1`，区分静态线索与真实请求。
- 已验证当前用户接口无 authx 但带浏览器会话可成功，无会话返回 401；不推广为所有接口规则。

## 下一轮最小任务

阶段 3 的真实音频核心链路已通过，继续阶段 4：先验收队列界面和自动曲间切换，再接入已核实接口的搜索与专辑功能。

OAuth、HLS 长时播放、CUE、远程路径与完整功能仍待后续。当前 FFmpeg 管道跳转较慢和容器随机读取限制见播放层说明。2026-09-13 按用户新要求提前归档 Electron；原生完整功能与发布审查仍未完成。

## 阶段 1A 验证

- `npm.cmd test`：65 项 headless 检查、15 项 Node 测试全部通过，其中新增诊断测试 5 项。PowerShell 限制 npm.ps1，因此使用 npm.cmd，未修改执行策略。
- `node scripts/check-privacy.js`：扫描 61 个文件，未发现规则覆盖的个人信息。
- `git diff --check`：通过；私人需求文件、原始探查缓存及调试配置的忽略规则已核对。
- 实际监听地址已核对为本机回环地址；本轮结束保留独立调试 Chrome 窗口供后续验证。未保存完整请求头或凭据输出。

## 工作区与会话

早前 Electron 重构修改仍保留，本轮未提交或推送。调试 Chrome 使用独立配置，仅本机调试端口；其中可能包含会话信息，禁止加入版本控制或打包。用户现有普通 Chrome 窗口未关闭。调试结束状态应在每轮交接中明确，不自动读取其他浏览器资料。

## 阶段 1B 本轮新增与审查

- 新增 `scripts/nas-media-probe.js` 及 8 项测试：非零 Range、静音播放与清理、范围不匹配、播放拒绝、媒体超时、认证失败、非法地址、清理异常及主动暂停竞态。
- 实测原始流 bytes=4096-5119 返回正确 206；浏览器读到时长、seek 成功且播放位置前进。测试播放器已清理，临时焦点模拟已关闭。
- 两次短暂 HLS 任务均成功创建和结束；初始化段与媒体段读取成功。其中一次全程不带 authx，未在项目中引入签名密钥。
- 页面刷新会话恢复成功；Cookie 属性证实为会话型，原生重启免登录仍必须验证。
- 第一轮核对真实协议与传输，第二轮检查诊断脚本的超时、凭据脱敏和释放路径；发现并修复主动暂停导致旧 play Promise 拒绝的误报，增加回归测试后重新实测成功。这不等于完成最终整项目的多轮发布审查。
- 最终验证：`npm.cmd test` 的 65 项 headless 检查与 23 项 Node 测试全部通过（合计 88 项）；隐私扫描覆盖 64 个文件，通过；`git diff --check` 通过。

## 阶段 2 验证

- 本地 SDK 10.0.401 已按微软发布 SHA-512 校验；WinUI 3 Release 构建为 0 警告、0 错误。
- 原生窗口中文资源显示正常，实际服务器连接成功。首次登录清理未创建目录的缺陷已修复；用户随后确认真实密码登录成功。
- 关闭并启动新的原生进程后，观察到“已恢复登录”，验证加密令牌保存、读取及服务器验证，不依赖浏览器 Cookie 数据库。
- 12 项 C# 测试通过。JavaScript 侧 65 项 headless 与 27 项 Node 测试通过；新增隐私扫描回归测试 4 项。隐私扫描已覆盖 C#/XAML/RESW 等原生源码，且不打印命中的敏感值。
- 构建入口为 `scripts/native.ps1`，依赖锁定；使用说明在 `native/README.md`。尚无安装包发布，旧 Electron 修改保留。

## 阶段 3 当前进展

- 用户指定 FFmpeg 解码，采用受控 HTTP 流 → FFmpeg 管道 → PCM → Windows 音频输出。详情和局限在 [FFmpeg 播放层](FFMPEG_PLAYBACK.md)。
- 原生窗口已加载真实分页曲库；真实 NAS 歌曲静音端到端测试通过：会话、曲库、播放进度前进、暂停位置不变、恢复、跳至 20 秒后继续、设备枚举、显式选择并回读设备、切回系统默认、停止并等待解码进程/流清理。
- 新增仅在 Debug 中启用的 `--verify-native-playback` 和 `--verify-synthetic-playback`。前者显式使用本应用已保存会话；后者仅生成内存测试音频。报告区分真实/合成，仅包含固定检查项和脱敏错误，不包含账号、地址、曲名或设备 ID。
- 正常 Windows 用户下全部原生测试通过，包括 DPAPI；沙箱设备枚举的 FileNotFoundException 在正常用户环境不复现。此前自动权限审核超时已通过后续实际运行解决。
- 该验证直接调用生产播放服务，不等同于已完成所有按钮、拖动和窗口尺寸的 UI 验收；最终发布回归仍保留这些项目。

## 阶段 4A：当前页播放队列

- 开始播放时固定当前页快照；翻页不改变已有队列。CUE 暂不加入自动续播，界面明确显示队列范围。
- 新增上一首/下一首、顺序播放、列表循环、单曲循环、随机播放及歌曲结束后的自动续播；单曲循环只影响自动结束，手动下一首仍切歌。
- 5 项队列规则测试通过；原生测试合计 28 项通过。自动切歌的界面完整回归待下一子阶段，不把领域逻辑测试替代 UI 实测。
- 初始流请求或曲库请求返回 401 时清空本地播放/队列并提示重新登录；普通网络错误不删除会话。
- 没有提交、推送或生成安装包。FFmpeg 固定版本及分发材料仍属于发布阶段工作。

## 2026-09-13：拖动修复与双击播放

- 修复进度滑块每次变化都立即跳转、同时被 500 ms 进度刷新写回的问题。拖动期间只预览，松手提交一次；键盘或点击变化合并提交，等待 SeekCompleted 时保持目标位置，超时显示明确提示。停止或换曲会撤销待提交操作。
- 已实现双击歌曲行播放，播放目标来自被双击行的数据，复用正常队列启动路径；列表空白区域不会重播旧选择。
- 使用 Debug `--preview-synthetic` 打开与生产相同的 XAML 和播放服务，默认静音且不读账户、不联网。实际鼠标拖动从约 6 秒到 30 秒，暂停状态保留，恢复后推进至 38 秒；双击 A 播放、双击 B 切换均通过。
- 本轮 28 项原生测试通过。此前仅验证底层跳转接口不足以覆盖 UI 拖动，本轮补充了真实指针交互检查。
- 用户已授权在合适的完整节点自主提交到本地 Git；对外发布仍依发布阶段执行，不由本地提交触发。
- 开发清单见 [原生开发清单](NATIVE_BACKLOG.md)。


## 2026-09-13：队列管理与 Electron 归档

- 队列管理弹窗支持查看、播放所选、移除和清空。删除当前曲目或清空时取消加载并停止播放器；删除其他曲目保持当前曲目身份，清除可能失效的随机历史索引。
- 新增队列移除、当前项删除、重新选择与非法 ID 回归。
- Release 构建通过（0 警告、0 错误）；正常 Windows 用户下 23 项原生检查通过（包含 DPAPI，本次未开启可选 FFmpeg 格式矩阵）；17 项 Node 辅助测试及隐私扫描通过。
- 新队列弹窗尚未完成真实窗口交互验收，自动续播与四种模式完整 UI 回归仍列为待办。
- 按用户本轮要求提前归档并移除活动 Electron 文件，见 [归档记录](ELECTRON_ARCHIVE.md)。源码与此前暂存差异有本机备份，旧发布工作流已移除，未发布安装包。

## 2026-09-14：队列交互验收完成当前子任务

- 使用真实合成音频窗口验证删除当前/非当前曲目、队列所选播放、清空重建及四种模式的基本续播行为；单曲循环在队列弹窗保持打开时也正常跨曲尾。
- 修复删除当前项后的队列 0/1 提示，并按选择与空队列状态禁用无效操作；修正后窗口已复测。
- Debug/Release 编译均为 0 警告、0 错误；23 项原生检查（含正常用户 DPAPI）通过，隐私扫描通过。未重跑可选 FFmpeg 格式矩阵。
- 详细范围及限制见 [队列窗口验收](QUEUE_UI_ACCEPTANCE.md)。下一任务：核实搜索接口并接入原生搜索；真实 NAS 大文件、网络错误与更广泛交互回归继续保留。

## 2026-09-15：歌曲搜索实现完成，真实接口与部分交互待验收

- 接入搜索、分页、刷新和清空恢复，搜索与播放请求取消隔离；旧查询结果不得覆盖新查询，搜索不改变已有队列。
- 26 项原生检查通过；Debug/Release 均为 0 警告、0 错误。合成窗口搜索 B、播放命中曲目、清空恢复曲库且保留队列已通过。
- 回车提交改为接收被 TextBox 处理过的路由事件，编译通过，修正后实机复测尚未完成。窗口捕获与目标不一致时已停止输入。
- 真实 NAS 搜索检查被自动审批拒绝，尚未执行；需用户明确授权使用本应用保存会话进行只读验证。详见 [搜索实现与验收](NATIVE_SEARCH.md)。

## 2026-09-15：真实搜索验证与分页修复

- 用户确认只读验证后，真实会话恢复、歌曲命中和空结果通过；发现当前 NAS 搜索忽略 page/size，重复返回完整集合。
- 原生仅对条数等于 total 的完整集合执行本地分页，保留服务端分页；拒绝不明确的超长部分响应，避免重复或漏歌。
- 修复后真实首/次页各一条且 ID 不同、总数一致，最终验证报告 stage=complete。报告仅含固定布尔检查项，进程退出与报告时间已核对。
- 28 项原生检查通过；独立 Debug 和标准 Release 均 0 警告、0 错误。已有测试窗口未关闭。回车及其余 UI 复测仍待完成，之后推进专辑/歌手详情。

## 2026-09-15：搜索窗口验收完成

- 新增仅 Debug 合成模式使用的多页与延迟请求夹具，共 122 首，不读取账户、不联网。
- 真实窗口已通过回车提交、111 首搜索结果三页往返、末页禁用、空结果提示及清空恢复。
- 20 秒旧请求故意忽略取消；约 9 秒时清空，约 29 秒时仍保持完整曲库，生产请求写回保护通过验收。
- 独立 Debug 与标准 Release 构建均 0 警告、0 错误。当前改动仅扩展 Debug 夹具，未重新运行上一节点已通过的 28 项核心检查。
- 详细证据见 NATIVE_SEARCH.md；更多输入法、真实连续请求和网络超时保留为扩展回归。下一开发子阶段为专辑/歌手与详情。

## 2026-09-15：专辑与歌手基础浏览

- 根据已审查前端契约接入两类列表、详情元数据和独立歌曲分页，详情歌曲复用现有播放与队列；返回列表保存页码。
- 新增 5 项契约检查，原生合计 33 项通过；Debug/Release 编译均 0 警告、0 错误。
- 合成窗口基本入口、详情返回、按钮播放、双击切歌和浏览时队列保留通过。测试使用本地合成内容，无 NAS 请求。
- 下一步核验真实 NAS 专辑/歌手接口与分页，补充多页、空详情和快速切换 UI 回归。关联专辑和封面等详情扩展仍待后续，详见 NATIVE_COLLECTIONS.md。
